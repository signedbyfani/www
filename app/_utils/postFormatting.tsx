import ReactDOMServer from "react-dom/server";
import Link from "@/app/components/Link";

export function getContentWithLinks(content: string) {
  // Simply return the content as is without link processing
  return content;
}